{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop permissions for PDF Parser.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
